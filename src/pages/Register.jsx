import Textbox from '../components/textBox/textBox.jsx';

export default function Register() {
    return (
        <div>
            <Textbox header={"TODO"} text={"This page will let the user sign in or create an account"} />
        </div>
    );
}